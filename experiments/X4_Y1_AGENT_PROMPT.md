Work directly in /home/kit/kimi-chip to advance only X4-Y1. Do not commit or push. First read experiments/MATRIX_CONTINUATION_PROMPT.md, experiments/KIMI_SPEC.md, experiments/5x5_status.json, experiments/MATRIX_PROGRESS.md, all X1-X3 trials.jsonl and DESIGN_Y*.md, and inspect their raw artifacts. X3 is now complete 5/5. Read the actual proven reference implementations rtl/int4_mac_v9.v, rtl/compute_core_v9.v, rtl/mac_array_v9.v and corresponding v10 files plus tb/tb_v9_v10.v before writing RTL.

Before implementing Y1, build machine-readable X4 failure-pattern ingestion tied to raw X1-X3 artifact paths, covering every terminal trial including successes, failures, and X3-Y5's separately preserved disk-full infrastructure failure. Build cumulative executable evidence-backed lint rules only from observed patterns. At minimum include the evidence-backed behavioral-SRAM, accumulator-overflow, exactly-once/double-accumulation, global shift-drain congestion, extracted max-capacitance, reset/restart/control-plane, artifact-disk-capacity, and marker/exit-code integrity patterns when the actual prior evidence supports each. Run the rules and save structured outputs.

Implement X4-Y1 as a real architectural replacement, not a wrapper around the current wide accumulator. Preserve the existing external x3-style module contract and registered SRAM-read protocol. Required structure is signed 12-bit per-cycle chunk accumulation and signed 24-bit wide accumulation, folding exactly every 16 valid products on a separate no-multiplier register-to-register cycle. Flush a nonempty tail before done. Each registered SRAM response must be accumulated exactly once. Keep 16-bit k_dim and K up to 65535. Handle K=0 correctly. Width proof is 16*64=1024 in signed 12b and 65535*64=4194240 in signed 24b.

Create latency-aware locked regressions that verify K=7, K=0, K=13, fold boundaries around 15/16/17 and 31/32/33, signed extrema including -8*-8 and mixed signs, tail flush, reset/abort and clean restart, exactly-once SRAM consumption, and adversarial K=65535. The K=65535 test must be practical but real simulation evidence, not width reasoning alone. Validate all outputs appropriate to each test. Run Icarus/Verilator and all cumulative executable lint rules.

Create X4-Y1 ORFS configuration at 752 MHz, approximately 1.329787 ns. Reuse the proven Nangate45 hard SRAM macro boundary and known-good remote ORFS setup. Synchronize only necessary files to kit@35.203.156.158 using ~/.ssh/gcs-vm. Before launch ensure at least 20 GB remote root free. Launch the full real flow in remote tmux x4-y1-route and Docker x4_y1_orfs with a robust marker preserving the actual pipeline/container return code. Update experiments/5x5_status.json and MATRIX_PROGRESS.md to active X4-Y1 with exact session/container/log/marker paths. Do not wait for the physical flow to finish once it is confirmed healthy and genuinely progressing. Do not design Y2 or X5. Leave a concise execution report in experiments/x4/AGENT_Y1_LAUNCH_REPORT.md. If blocked, record the exact blocker in status/progress and exit nonzero. Never fabricate metrics.