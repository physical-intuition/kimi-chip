# Kimi 5x5 Matrix Progress

Updated 2026-08-11 01:23 UTC.

X1 is complete. Y1 failed because its 256-word shift-drain network could not converge in global routing. Y2 passed after changing to row-local banked accumulators. Y3 reached 463.49 MHz but retained 16 extracted max-capacitance violations. Y4 applied `CAP_MARGIN=20` and passed all extracted checks at 451.475 MHz and 0.177508 mm². Y5 replaced selected-bank shifting with indexed non-shifting readout and passed all extracted checks at 455.449 MHz and 0.167356 mm², with zero DRC and antenna violations. Exact metrics and raw evidence are in `x1/trials.jsonl`, `x1/DESIGN_Y*.md`, `x1/artifacts/y5`, and the remote ORFS tree.

X2-Y1 is terminal and recorded exactly once as a strict-signoff failure. Its local K=7, K=0, and K=13 regression passed all 768 outputs, and all three executable X2 rules passed. Full Nangate45 ORFS completed with zero DRC, zero antenna violations, and 469.734 MHz extracted fmax, but final extraction found one max-capacitance violation under `CAP_MARGIN=20`.

X2-Y2 is terminal and recorded exactly once as a strict-signoff pass. Raising `CAP_MARGIN` to 30 preserved the 768/768 functional regression and all three executable X2 gates, then closed at 467.594 MHz and 0.171307 mm² with zero extracted setup, hold, slew, fanout, and max-capacitance violations. Detailed routing reduced 30,927 initial violations to zero in five iterations; final DRC and antenna counts are zero. Relative to Y1, the stronger margin costs 0.49% routed area and 0.46% fmax. Raw evidence is preserved in `x2/artifacts/y2`.

X2-Y3 is terminal and recorded exactly once as a strict-signoff pass. `CAP_MARGIN=25` closed at 468.346 MHz and 0.170822 mm² with zero extracted setup, hold, slew, fanout, and max-capacitance violations, zero DRC, and zero antenna violations. It improves both fmax and area over margin 30. Raw evidence is preserved in `x2/artifacts/y3`.

X2-Y4 is terminal and recorded exactly once as a strict-signoff pass. `CAP_MARGIN=22` closed at 470.547 MHz and 0.170601 mm² with 0.374812 ns setup slack, 0.00309652 ns hold slack, zero extracted setup, hold, slew, fanout, and max-capacitance violations, zero DRC, and zero antenna violations. Detailed routing reduced 30,724 initial violations to zero in four repair iterations. Relative to margin 25, it gains 2.201 MHz and removes 221 um². Raw evidence is preserved in `x2/artifacts/y4`.

X2-Y5 is terminal and recorded exactly once as a strict-signoff pass. `CAP_MARGIN=21` closed at 468.201 MHz and 0.170536 mm² with 0.364167 ns setup slack, 0.00331735 ns hold slack, zero extracted setup, hold, slew, fanout, and max-capacitance violations, zero DRC, and zero antenna violations. Detailed routing reduced 29,754 initial violations to zero in five repair iterations. Margin 21 is the minimum tested clean integer boundary, although margin 22 remains the higher-fmax X2 point. Raw evidence is preserved in `x2/artifacts/y5`.

X2 is complete. X3-Y1 added a hash-locked deterministic K=7/K=0/K=13 testbench and a durable `FAILED_DIRECTIONS.md` covering the global shift-drain failure, extracted max-capacitance misses, over-conservative repair margin, and behavioral-SRAM trap. The locked regression passed all 768 outputs and all three executable X2 lint gates passed. Its real full Nangate45 flow at `CAP_MARGIN=21` passed strict signoff at 468.201 MHz and 0.170536 mm², with 0.364167 ns setup slack, 0.00331735 ns hold slack, zero extracted setup/hold/slew/fanout/max-cap violations, zero DRC, and zero antenna violations. Raw evidence is preserved in `x3/artifacts/y1`.

X3-Y2 is terminal and recorded exactly once as a strict-signoff pass. It independently locks an adversarial signed-extrema pattern using alternating INT4 -8 and +7 values across every lane. Its SHA-256-locked K=7/K=0/K=13 regression passed all 768 outputs and all three executable lint gates passed. The real full Nangate45 flow at `CAP_MARGIN=21` closed at 468.201 MHz and 0.170536 mm² with 0.364167 ns setup slack, 0.00331735 ns hold slack, zero extracted setup/hold/slew/fanout/max-cap violations, zero DRC, and zero antenna violations. Detailed routing reduced 29,754 initial violations to zero in five repair iterations. Raw evidence is preserved in `x3/artifacts/y2`.

X3-Y3 locks a sparse two-point signed basis per K lane, using distinct row and column strides to expose lane and output-address permutations that dense repeating patterns can mask. Its SHA-256-locked K=7/K=0/K=13 regression passed all 768 outputs and all three executable lint gates passed. The real full Nangate45 flow at `CAP_MARGIN=21` is launching remotely in tmux `x3-y3-route`, Docker `x3_y3_orfs`.

X3-Y3 is terminal and recorded exactly once as a strict-signoff pass. Its sparse two-point signed basis exposes row, column, lane, and output-address permutations; the SHA-256-locked K=7/K=0/K=13 regression passed all 768 outputs and all three executable lint gates passed. The real full Nangate45 flow at `CAP_MARGIN=21` closed at 468.201 MHz and 0.170536 mm² with 0.364167 ns setup slack, 0.00331735 ns hold slack, zero extracted setup, hold, slew, fanout, and max-capacitance violations, zero DRC, and zero antenna violations. Detailed routing reduced 29,754 initial violations to zero in five repair iterations. Raw evidence is preserved in `x3/artifacts/y3`.

X3-Y4 adds a hash-locked control-plane regression with nonlinear signed stimulus, paired-memory-request assertions, unique output-address coverage, `k_dim` mutation after launch, and a spurious `start` pulse while busy. Its local K=7/K=0/K=13 regression passed all 768 unique outputs and all three executable lint gates passed. The real full Nangate45 flow at `CAP_MARGIN=21` is launching remotely in tmux `x3-y4-route`, Docker `x3_y4_orfs`.

X3-Y4 is terminal and recorded exactly once as a strict-signoff pass. The real full Nangate45 flow at `CAP_MARGIN=21` closed at 468.201 MHz and 0.170536 mm² with 0.364167 ns setup slack, 0.00331735 ns hold slack, zero extracted setup, hold, slew, fanout, and max-capacitance violations, zero DRC, and zero antenna violations. Detailed routing reduced 29,754 initial violations to zero in five repair iterations. The launch wrapper wrote an empty `FLOW_RC` marker, which is preserved rather than repaired or invented; terminal success is backed by the complete final GDS/report sequence and zero errors in final reporting. Raw evidence is preserved in `x3/artifacts/y4`.

X3-Y5 adds a hash-locked mid-run asynchronous-reset abort followed by quiescence assertions and a clean required K=7/K=0/K=13 restart. Its local regression passed all 768 post-restart outputs and all three executable lint gates passed. The first real full Nangate45 attempt reached zero detailed-route violations and zero antenna violations, then failed with `FLOW_RC=2` while writing `5_2_route.odb` because the GCS root filesystem had only 1.1 MB free. That infrastructure-failure evidence is preserved under `x3/artifacts/y5_failed_disk_full`. Historical generated checkpoints were removed only after their trial evidence was already preserved, freeing 34 GB, and the same cell is retrying remotely in tmux `x3-y5-route`, Docker `x3_y5_orfs`.


X3-Y5 is terminal and recorded exactly once as a strict-signoff pass. Its SHA-256-locked mid-run asynchronous-reset abort, post-abort quiescence, and clean-restart regression passed all 768 post-restart K=7/K=0/K=13 outputs, and all three executable lint gates passed. The first remote attempt failed after routing because the GCS root filesystem filled while writing `5_2_route.odb`; that infrastructure evidence remains preserved separately. The clean retry completed with `FLOW_RC=0` at 468.201 MHz and 0.170536 mm², with 0.364167 ns setup slack, 0.00331735 ns hold slack, zero extracted setup, hold, slew, fanout, and max-capacitance violations, zero DRC, and zero antenna violations. Detailed routing reduced 29,754 initial violations to zero in five repair iterations. Raw successful evidence is preserved in `x3/artifacts/y5`. X3 is complete.

X4 is pending prerequisite ingestion of every X1-X3 trial and raw artifact into machine-readable failure patterns and cumulative executable evidence-backed lint rules. X4-Y1 must then implement the proven signed 12-to-24-bit hierarchical fold at a 752 MHz target; X5 remains untouched.

X3 is complete 15/15. X4-Y1 ingested all X1-X3 trial history plus 610 locally preserved raw artifact files and their SHA-256 evidence into `x4/harness/history_patterns.json`. Eight cumulative executable evidence-backed rules pass, covering the prior routing, electrical, SRAM-boundary, overflow, exactly-once, control-plane, disk-capacity, and marker-integrity failures. Its new RTL replaces the wide per-cycle accumulator with signed 12-bit chunks folded every 16 products into signed 24-bit state on a separate cycle. Local Icarus, Verilator, Yosys, and executable lint checks pass. Functional simulation covers K=7/0/13, fold boundaries K=15/16/17/31/32/33, signed extrema, busy-start and k_dim latching, reset-abort and clean restart, paired exactly-once registered SRAM responses, tail flush, and full-range K=65535. The real full Nangate45 flow is active on GCS in tmux `x4-y1-route`, Docker `x4_y1_orfs`, at the 752 MHz / 1.329787 ns target. GCS has about 32 GB free. X5 remains unplanned and pending.

X4-Y1 first physical attempt completed through final GDS generation, with 28,913 initial detailed-route violations repaired to zero, 3,436,326 um final wire, 985,696 vias, and zero antenna net/pin violations. Its launch wrapper was externally terminated after `make` returned but before writing a valid marker, and the transient ORFS final timing/report directory disappeared before preservation. No terminal timing result is claimed. The identical 752 MHz cell is rerunning in `x4-y1-route` / `x4_y1_orfs`; the container now copies raw logs, reports, results, and objects into `x4/artifacts/y1` before it exits, preventing a second evidence-loss window. At 2026-08-11 03:43 UTC, global routing had completed and detailed-routing iteration 0 was active with zero violations at 10%; the marker remained absent, the container and tmux session were healthy, and 31 GB root disk remained free. X5 remains untouched.

X4-Y1 is terminal and recorded exactly once as a strict-signoff FAIL. The hierarchical 12-to-24-bit fold passed the full functional suite, and the real 752 MHz Nangate45 flow completed with zero route, DRC, antenna, hold, slew, fanout, or capacitance violations. It missed setup with 719.14 MHz extracted fmax, -0.06075 ns WNS, -0.121209 ns TNS, and two violating request outputs. The worst path is a 15-stage control cone from `issued[0]` through the redundant progress comparator to `weight_req`, not the accumulator. Raw evidence is preserved under `x4/artifacts/y1`. Y2 will remove that redundant predicate and target 760 MHz based on the next datapath path headroom. X5 remains untouched.

X4-Y2 is terminal and recorded exactly once as a strict-signoff PASS. Removing the redundant request progress comparator closed the real full Nangate45 flow at 760 MHz with 783.569 MHz extracted fmax, +0.0395887 ns setup WNS, +0.0388065 ns hold WNS, zero extracted setup/hold/slew/fanout/max-cap violations, zero DRC, and zero antenna violations. The new report-proven bottleneck is an 18-cell combined row/column readout path from `drain_col[0]` to `out_wdata[12]`. Correct raw evidence is preserved under `x4/artifacts/y2`; Y3 preparation will split this selector across registered stages. X5 remains untouched.

X4-Y3 preparation initially encountered unavailable coding CLI credentials. The matrix owner continued directly from the preserved Y2 evidence without altering Y1/Y2 artifacts. Y3 splits the report-proven 18-cell combined row/column selector by registering the selected accumulator row in `S_DRAIN_LOAD`, then selecting its column in `S_DRAIN_WRITE`. This adds 16 row-load cycles while preserving output count, order, interface, and arithmetic. All 14 cumulative executable lint/proof checks pass, along with Icarus functional regression through K=65535, Verilator lint, and Yosys structural check. The 780 MHz target is above Y2's 760 MHz target and just below its measured 783.569 MHz extracted ceiling. The real full Nangate45 flow is launching remotely in tmux `x4-y3-route`, Docker `x4_y3_orfs`. X5 remains untouched.

X4-Y3 is terminal and recorded exactly once as a strict-signoff PASS. Registering the selected accumulator row removed Y2's combined readout path. The real 780 MHz Nangate45 flow reached 837.683 MHz extracted fmax with +0.0883312 ns setup WNS, +0.0369844 ns hold WNS, zero extracted setup/hold/slew/fanout/max-cap violations, zero DRC, and zero antenna violations. The new 21-cell worst path is the signed 24-bit fold carry chain from `chunk_accum[57][1]` to `wide_accum[57][16]`. Raw reports are preserved locally under `x4/artifacts/y3`, and the full artifact tree remains on the GCS host. Y4 splits that fold into registered low/high 12-bit slices and targets 820 MHz. X5 remains untouched.

X4-Y4 is terminal and recorded exactly once as a strict-signoff FAIL. Its registered low/high 12-bit fold split passed the full inherited functional suite and raised extracted fmax to 900.93 MHz at the 820 MHz target. Setup WNS is +0.109536 ns, hold WNS is +0.0229796 ns, and setup, hold, slew, fanout, DRC, and antenna checks are clean. Strict signoff failed on two extracted max-capacitance violations: `wire57969/Z` is 356.23 against a 242.31 limit and `wire57973/Z` is 270.23 against 242.31. The new worst setup path is the 12-bit chunk accumulator from `chunk_accum[186][0]` to `chunk_accum[186][11]`. Raw reports and logs are preserved locally under `x4/artifacts/y4`; the complete artifact tree remains on GCS.

X4-Y5 preserves the Y4 architecture and applies the report-driven physical-design repair `CAP_MARGIN=30`, up from 22, to address those two extracted capacitance violations. Its 880 MHz target is monotonically above 820 MHz and below Y4's measured 900.93 MHz ceiling. All 18 cumulative executable checks, the full K=7/0/13 and boundary/extrema/reset regression through K=65,535, Verilator lint, and Yosys structural checking pass locally. An initial launch failed immediately before synthesis because the new RTL was synced to the wrong remote directory; no physical result was produced. The path was corrected and the canonical Y5 flow completed with `FLOW_RC=0`.

X4-Y5 is terminal and recorded exactly once as a strict-signoff PASS. Final extraction achieved 909.621 MHz at the 880 MHz target, with +0.037041 ns setup WNS, zero setup TNS, +0.0292948 ns hold WNS, and zero setup, hold, slew, fanout, or max-capacitance violations. Detailed routing reduced 26,192 initial violations to zero in four repair iterations; final DRC and antenna net/pin counts are zero. The final design uses 205,474 um² of standard-cell area, 3,376,130 um of wire, 985,858 vias, and an estimated 0.367562 W. The worst path is a 19-cell cone from `chunk_accum[156][1]` to `wide_accum[156][11]`. Canonical reports and timing analysis are preserved locally under `x4/artifacts/y5`; the full artifact tree remains on the GCS host.

X4 is complete 5/5 and the matrix is complete through X4, 20/25 cells total. The trajectory was 719.140 MHz FAIL at 752, 783.569 MHz PASS at 760, 837.683 MHz PASS at 780, 900.930 MHz electrical FAIL at 820, and 909.621 MHz PASS at 880. The cumulative harness ends with 18 executable evidence-backed checks. Full synthesis is in `x4/X4_SUMMARY.md`. X5 remains untouched and pending by protocol.

X5-Y1 began only after machine-ingesting all five terminal X4 cells and hashing their raw evidence. The harness verified the causal RTL/config trajectory, then identified X4-Y5's final 19-cell max setup cone from `chunk_accum[156][1]` to `wide_accum[156][11]` as the remaining 12-bit low-fold carry chain. It derived 937.092 MHz from X4's measured fold-split gain, then generated the clock policy. Y1 splits the low fold into registered 6-bit slices while preserving every prior X4 repair. Full local regression through K=65,535, provenance/structural lint, Verilator, and Yosys pass. The real Nangate45 flow is launching in remote tmux `x5-y1-route`, Docker `x5_y1_orfs`. X5-Y2 through Y5 do not exist and remain unplanned.

X5-Y2 started only after X5-Y1 terminal extraction. It holds the failed 937.092 MHz target and registers the 8-bit product before a MAC-commit stage; request, response/product capture, and commit overlap. Local full regression and structural/tool checks must pass before remote launch. X5-Y3 through Y5 remain unplanned.

X5-Y3 started from X5-Y2 terminal evidence. It preserves the MAC pipeline, removes the wide comparator from request outputs using registered requests and a 5-bit burst budget, raises CAP_MARGIN to 50, and derives a 930.359 MHz target from half the historical X4 comparator-removal recovery. Local regression and tool checks pass. Y4-Y5 remain unplanned.

X5-Y4 started only after X5-Y3 terminal evidence. Y3 extracted 988.581 MHz with +0.0633 ns WNS. Its limiting path was state[1] to busy; Y4 registers busy. The next internal product_pipe-to-chunk_accum path's +0.14 ns slack implies ~1069.686 MHz. The midpoint recovery exceeds 1 GHz, so Y4 is capped at the progressive 1 GHz milestone. Local regression and tool checks pass. Y5 remains unplanned.

X5-Y4 has no terminal physical result because `factory-v0` repeatedly hard-reset during detailed routing. Kernel boot evidence identifies the prior reset reason as `thermal pin BP_THERMTRIP_L was tripped`. Attempts used one OpenROAD thread and Docker CPU caps from 1.0 down to 0.3; the final retry used a 50% utilization floorplan and remained clean through 10% of detailed routing with zero violations before another thermal reset. OpenROAD reported about 2.93 GB at that point, so the evidence supports a host thermal blocker rather than ordinary OOM or a design failure. Interrupted logs, reports, and status are preserved under `x5/artifacts/y4_interrupted`; no fmax, final setup/hold, final DRC/antenna/cap result, pass/fail claim, or Y5 preparation is recorded.
