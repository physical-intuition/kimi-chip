Work only in /home/kit/kimi-chip and do not commit or push. Read experiments/MATRIX_CONTINUATION_PROMPT.md, experiments/x4/DESIGN_Y3.md, experiments/x4/artifacts/y3/timing_analysis.json, experiments/x4/rtl/x4_y3_kimi.v, its testbench, run script, lint script, and flow config first.

Implement X4-Y4 as an evidence-driven timing repair. Y3 strict-passed 780 MHz with extracted fmax 837.683 MHz, setup WNS +0.0883312 ns, hold WNS +0.0369844 ns, all physical violations zero. Its new worst path is chunk_accum[57][1] to wide_accum[57][16], 21 combinational cells. The raw path shows the 24-bit fold carry chain. Split the fold addition into two real registered 12-bit slices across separate FSM cycles, first compute/commit the low 12 bits and register carry, then compute/commit the high 12 bits with signed arithmetic and carry. Preserve exact signed 24-bit two's-complement behavior for positive and negative chunks, all 256 accumulators, the external interface, output order/count, exactly-once registered SRAM consumption, K=65535, tail flush, reset/restart, and all inherited regressions. Do not reintroduce a multiplier on fold cycles. Make the RTL clear and synthesizable Verilog.

Create x4_y4 files by deriving from Y3: rtl/x4_y4_kimi.v, tb/tb_x4_y4.v, harness/lint_x4_y4.py, run_y4.sh, flow/config_y4.mk, flow/constraint_y4.sdc, launch_remote_y4.sh, harness/policy_y4.json, and DESIGN_Y4.md. Add cumulative executable lint checks proving two registered fold slices and rejecting a direct chunk_accum-to-wide_accum[23:12] single-cycle fold. Target 820 MHz, period 1.219512 ns. This is monotonically above 780 and below Y3 measured 837.683 MHz ceiling. Run local Icarus functional simulation, Verilator lint, Yosys check, and executable lint. Fix all issues until local validation passes. The remote wrapper must preserve its own x4_y4_kimi artifacts atomically before exit, check at least 10 GB remote disk, write .y4_flow_rc correctly, and must not touch any X5 files. Do not launch the remote flow. Write a concise terminal local result into DESIGN_Y4.md.