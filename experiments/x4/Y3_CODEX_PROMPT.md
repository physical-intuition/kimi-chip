Work directly in /home/kit/kimi-chip. Read experiments/MATRIX_CONTINUATION_PROMPT.md first and obey it. Do not commit or push. X1-X3 and X4-Y1/Y2 are terminal; do not alter their preserved evidence. Focus only on preparing X4-Y3, and do not design X4-Y4/Y5 or anything in X5.

Read experiments/x4/artifacts/y2/timing_analysis.json, DESIGN_Y2.md, trials.jsonl, all current X4 RTL/testbench/lint/flow/launch scripts, and raw Y2 reports. Y2 strict-passed 760 MHz with extracted fmax 783.569 MHz. Its worst path is drain_col[0] to out_wdata[12], an 18-combinational-cell combined row/column readout selector. Make a genuine report-driven RTL improvement by splitting this readout selection across a registered stage, preserving the external module interface and exact signed INT4 results. Derive a monotonically higher Y3 frequency target from Y2 extracted headroom and the new expected stage boundary, record the reasoning, and do not select arbitrary later targets.

Create the complete Y3 cell files by copying only what is structurally necessary from Y2 and making the actual pipeline change: experiments/x4/rtl/x4_y3_kimi.v, latency-aware tb/tb_x4_y3.v, cumulative evidence-backed executable lint_x4_y3.py and policy_y3.json, flow config_y3.mk and constraint_y3.sdc, run_y3.sh, launch_remote_y3.sh, and initial DESIGN_Y3.md. Add a cumulative lint rule that checks the report-proven combined readout selector is split by a register and rejects the old direct drain_col-to-out_wdata structure. Ensure the remote wrapper points only to x4_y3 paths and atomically preserves raw logs, reports, results, objects, and flow RC before container exit.

Run the full local Y3 validation suite including Icarus functional tests, Verilator lint, Yosys synthesis/check, all cumulative executable lint rules, and the full K=65535 adversarial regression inherited from Y2. Fix any failures. Do not launch the remote ORFS flow yet. At the end, summarize files changed, exact target and evidence-based derivation, pipeline latency change, and local validation results.